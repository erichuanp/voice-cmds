name: voice-cmds
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - PySide6>=6.6
      - sounddevice
      - numpy
      - sherpa-onnx
      - tokenizers
      - pypinyin
      - pywin32
      - requests
      - tqdm
